airbnb:
  target: prod
  outputs:
    prod:
      type: snowflake
      account: XJVCLEX-NNC43628
      user: DBT_SVC
      role: DBT_ROLE
      database: ANALYTICS_DB
      warehouse: TRANSFORM_WH
      schema: MARTS
      threads: 8
      authenticator: workload_identity
      workload_identity_provider: OIDC
      token: "{{ env_var('SNOWFLAKE_WORKLOAD_IDENTITY_TOKEN') }}"
    dev:
      type: snowflake
      account: XJVCLEX-NNC43628
      user: "{{ env_var('SNOWFLAKE_DEV_USER') }}"
      authenticator: snowflake_jwt
      private_key_path: "{{ env_var('SNOWFLAKE_DEV_PRIVATE_KEY_PATH') }}"
      role: DBT_ROLE
      database: ANALYTICS_DB
      warehouse: TRANSFORM_WH
      schema: MARTS
      threads: 8